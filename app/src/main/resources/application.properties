spring.application.name=app

spring.datasource.url=jdbc:mysql://localhost:3306/ecommerce
spring.datasource.username=root
spring.datasource.password=vinay

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=validate

# Generate a strong secret: openssl rand -base64 64
jwt.secret=eWVib2hvdGFhY2hhYWFkbWloYWlpdXNpbGl5ZWFhY2hhaGFp7
jwt.expiration-ms=345600000
jwt.refresh-expiry-ms=604800000
jwt.refresh-cookie-expiry-seconds=604800

stripe.api.key={STRIPE_API_KEY}
stripe.webhook.secret={STRIPE_WEBHOOK_SECRET}
stripe.publishable.key={STRIPE_PUBLISHABLE_KEY}

paypal.client.id={PAYPAL_CLIENT_ID}
paypal.client.secret={PAYPAL_CLIENT_SECRET}
paypal.mode=sandbox


server.port=8080

# Actuator ? expose health check endpoint
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized

logging.level.com.ecommerce=INFO
logging.level.org.springframework.security=WARN